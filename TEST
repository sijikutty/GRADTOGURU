import json

# Example data (replace with your actual user credentials)
user_credentials = {
    "user1": "password1",
    "user2": "password2",
    "user3": "password3"
}

# File path where you want to save the JSON file
json_file_path = "user_credentials.json"

# Write JSON data to file
with open(json_file_path, 'w') as json_file:
    json.dump(user_credentials, json_file, indent=4)

print(f"User credentials saved to {json_file_path}")
