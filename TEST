frame = iframe_element.content_frame()
        if frame is None:
            print("Failed to get content frame from the iframe")
            return None
        
        # Get the outer HTML of the element inside the iframe
        outer_html = frame.evaluate(f'document.querySelector("{selector}").outerHTML')
