Set WshShell = CreateObject("WScript.Shell")

' Execute the command and capture the process exit code
returnCode = WshShell.Run("cmd.exe /c python dynamic_API.py", 0, True)

' Wait for 10 seconds (if needed)
wait(10)

' Display the process exit code in a message box
MsgBox returnCode
