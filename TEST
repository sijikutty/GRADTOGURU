    parameters = parameter_definitions.GetParameters()

    # Print parameter names
    if parameters:
        print("Available parameters in the test:")
        for parameter in parameters:
            print(parameter.Name)
    else:
        print("No parameters are defined in the test.")
