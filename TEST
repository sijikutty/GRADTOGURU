LogFl=$FIT_LOG/$1 
chmod 755 $LogFl
sqlfile=$FIT_SQL/max_count.sql
ConnStr=`cat $FIT_SCRIPT/$2`
Start_time=`date +%H%M%S`
End_time=190000
src_name=`echo $1 | cut -d. -f1`
check_count()
{
	MAX_COUNT=`sqlplus -s $ConnStr << EOF
	@$sqlfile
	EXIT;
	EOF`
	if [ $? -ne 0 ] ; then
	 echo -e "\nDatabase error" >> $LogFl
