# Wait for the "Enable Content" button to appear
time.sleep(5)  # Adjust this delay as needed

# Click the "Enable Content" button
if app.top_window().window(title="Enable Content").exists(timeout=10):
    enable_content_dialog = app.top_window().window(title="Enable Content")
    enable_content_dialog.click()
