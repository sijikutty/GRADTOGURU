# Wait for up to 20 seconds for the warning to appear
page.wait_for_selector("text='Warning -- No ChartFields'", timeout=20000)  # 20 seconds

# Check if the warning is visible, and if so, click the OK button
if page.get_by_text("Warning -- No ChartFields", strict=False).is_visible():
    page.get_by_role("button", name="OK").click()
