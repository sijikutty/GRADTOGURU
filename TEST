' Create a function to load environment variables from JSON
Function LoadEnvironmentVariablesFromJSON(jsonFilePath)
    Dim fso, jsonFile, jsonString, jsonObject
    Set fso = CreateObject("Scripting.FileSystemObject")
    
    ' Open the JSON file
    Set jsonFile = fso.OpenTextFile(jsonFilePath)
    
    ' Read the contents of the JSON file
    jsonString = jsonFile.ReadAll
    
    ' Close the file
    jsonFile.Close
    
    ' Parse the JSON string to a JSON object
    Set jsonObject = JsonConverter.ParseJson(jsonString)
    
    ' Iterate through each property in the JSON object
    For Each property In jsonObject
        ' Set the environment variable
        Environment.Value(property) = jsonObject(property)
    Next
End Function

' Call the function to load environment variables before each test
LoadEnvironmentVariablesFromJSON "path/to/your/environment_variables.json"
