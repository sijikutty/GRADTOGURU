' Print details of each control in the Ribbon
For i = 1 To controls.Count
    ' Get the control at the current index
    Set ctrl = controls.Item(i)
    ' Output details of the control to the test results
    Reporter.ReportEvent micDone, "Control Details", "Control: " & ctrl.Caption & ", ID: " & ctrl.ID
Next
