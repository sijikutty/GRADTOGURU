            # Get all matching elements
            view_links = frame.query_selector_all(view_link_selector)
            
            # Print the number of matching elements
            print(f"Number of elements matching the selector '{view_link_selector}': {len(view_links)}")
            
