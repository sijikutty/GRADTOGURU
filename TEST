json_string = json.dumps(json_data)

# Path to the UFT script folder
uft_script_folder = r'C:\path\to\uft_script_folder'

# Execute UFT script from the folder, passing JSON string as command-line argument
subprocess.run(['cscript', os.path.join(uft_script_folder, 'main_script.vbs'), json_string])
