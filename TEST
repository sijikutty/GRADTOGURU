import boto3 
from botocore.exceptions import ClientError
import sys
import json

def upload_file(temp_file, bucket, s3_filename):
    # Uploading the file to S3
    try:
        session = boto3.Session(profile_name='dev')
        s3_client = session.client('s3')

        response = s3_client.upload_file(temp_file, bucket, s3_filename)
        print(response)

    except ClientError as e:
        print(e)
        return False
    
    return True

if __name__ == '__main__':
    # Getting command line arguments
    temp_file = sys.argv[1]
    s3bucket = sys.argv[2]
    s3_pathname = sys.argv[3]

    # Upload the file and print the response
    response = upload_file(temp_file, s3bucket, s3_pathname)
    print(response)

-------------
' Construct the command string
    Dim command
    command = """" & pythonExePath & """ """ & scriptPath & """ """ & tempFile & """ """ & s3bucket & """ """ & s3filename & """"
    
    ' Execute the command using SystemUtil.Run
    Dim output
    Dim success
    success = SystemUtil.Run(command, 1, True)
    
    ' Check if the Python script executed successfully
    If success Then
        MsgBox "Python script executed successfully."
    Else
        MsgBox "Failed to execute Python script."
    End If
    
    ' Your existing code...
