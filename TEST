' Include the JsonUtils function library
ExecuteFile "path_to_your_function_library\JsonUtils"

' Replace "path_to_your_credentials_file\credentials.json" with the actual path to your credentials JSON file
Dim jsonFilePath
jsonFilePath = "path_to_your_credentials_file\credentials.json"

' Read JSON from file
Dim credentials
Set credentials = ReadJsonFromFile(jsonFilePath)

' Extract username and password
Dim username, password
username = credentials("username")
password = credentials("password")

' Now you can use the username and password as needed
