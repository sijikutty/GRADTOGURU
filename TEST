    # Find the first row of the table
    first_row = page.locator('.table-class tbody tr:nth-child(1)')

    # Find the hyperlink within the first row
    hyperlink = first_row.locator('a')

    # Click the hyperlink
    hyperlink.click()
