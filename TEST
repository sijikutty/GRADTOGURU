try:
        tree = ET.parse(xml_file_path)
        root = tree.getroot()

        for node in root.iter("node"):  # Assuming "node" is the XML element containing the result data
            if node.attrib.get("name") == "Fail":
                description = node.find("description").text
