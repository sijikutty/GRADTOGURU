# Get the "Global" sheet
global_sheet = data_table.GetSheet("Global")

# Add a new row
global_sheet.AddRow()

# Set values in the new row
global_sheet.SetCell(1, "YourValue1")  # Column index starts from 1
global_sheet.SetCell(2, "YourValue2")
