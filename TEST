# Find all child windows
child_windows = main_window.children()

# Print titles and properties of all child windows
for i, window in enumerate(child_windows):
    print(f"Element {i+1}:")
    print(window.dump_tree())
    print("-" * 50)
