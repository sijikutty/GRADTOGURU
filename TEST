    frame.get_by_role("button", name="Refresh").click()

    run_status = frame.locator("[id=\"PMN_PRCSLIST_RUNSTATUSDESCR\\$0\"]").inner_text

    # Wait for Run status to be Success/Not Success/Warning/Error for approx 10min.
    retry = 0
    while retry < 600:
        expected_values=['Success','Error','Warning','No Success']

        if run_status.text_content() in expected_values:
            break

        else:
            frame.get_by_role("button", name="Refresh").click() 
            retry += 1

    if run_status.text_content().lower() != "success":
        raise Exception(f"Process Status : {run_status.text_content()}")

---
	Browser("Browser").Page("Data Migration Workbench").Frame("TargetContent_3").WebButton("Refresh").Click

Browser("Browser").Page("Data Migration Workbench").Frame("TargetContent_3").WebElement("PMN_PRCSLIST_RUNSTATUSDESCR$0").innertext()
	
