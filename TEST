 # Wait for the download to complete (you may need to adjust the timeout)
        download = page.wait_for_download()

        # Get the downloaded file path
        downloaded_file_path = download.path()

        # Wait for the file to be fully downloaded (you may need to adjust the timeout)
        timeout = 30  # Timeout in seconds
        while not os.path.exists(downloaded_file_path):
            if timeout <= 0:
                print("Timeout: File download took too long.")
                break
            time.sleep(1)
            timeout -= 1

        # Check if the file exists
        if os.path.exists(downloaded_file_path):
            print("File downloaded successfully!")
        else:
            print("File download failed!")
