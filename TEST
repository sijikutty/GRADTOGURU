Object required: '[string: "{
  "fahusername": "]'

Line (10): "Set jsonContents = CreateObject("Scripting.FileSystemObject").OpenTextFile(jsonFilePath).ReadAll()".

Dim jsonContents, parsedJson
    Set jsonContents = CreateObject("Scripting.FileSystemObject").OpenTextFile(jsonFilePath).ReadAll()
    Set parsedJson = JsonUtil.Parse(jsonContents)
    
    Set ReadJsonFromFile = parsedJson
' Replace "path_to_your_credentials_file\credentials.json" with the actual path to your credentials JSON file
Dim jsonFilePath
jsonFilePath = "path_to_your_credentials_file\credentials.json"

' Read JSON from file and fetch username and password
Dim result
Set result = ReadJsonFromFile(jsonFilePath)

' Extract username and password
Dim username, password
username = result("username")
password = result("password")
