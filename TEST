popup_dialog = page.frame_locator("iframe[id='unique-frame-id']")

# Wait for the iframe containing specific text to be visible
page.wait_for_selector("iframe:has-text('Specific Text in Frame')")
popup_dialog = page.frame_locator("iframe:has-text('Specific Text in Frame')")

# Locate the iframe within a specific section or container
container = page.locator("#container-id")  # Replace with actual container identifier
iframe = container.frame_locator("iframe[name='frame-name']")

# Locate iframe by its title attribute
popup_dialog = page.frame_locator("iframe[title='Frame Title']")

 # Define the selector for the iframe containing specific text
    iframe_selector = "iframe:has-text('Specific Text in Frame')"
    
    # Wait for the iframe to be visible
    page.wait_for_selector(iframe_selector, timeout=10000)  # Adjust timeout as needed
    
    # Check if the iframe exists and get the iframe locator
    iframe_locators = page.locator(iframe_selector)
    
    # Determine if there are any matching iframes
    if iframe_locators.count() > 0:
        print("Iframe with specific text is present.")
        # Optionally, interact with the iframe
        iframe = page.frame_locator(iframe_selector)
        # Perform actions within the iframe if needed
        # For example: print the text content
        content_text = iframe.locator('body').text_content()
        print(f"Text content in the iframe: {content_text.strip()}")
    else:
        print("No iframe with the specified text was found.")
