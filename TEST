        link_text = 'More information...'  # Replace with the text of the link you're checking
        count = page.locator(f'text={link_text}').count()

        if count > 0:
            print('Link is present on the page.')
        else:
            print('Link is not present on the page.')
