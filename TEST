    if Status.upper() != "N" :
        #raise Exception(f"Job Status : {Job_status}")
        raise Exception(f"Autosys job {sub_autosys_job} has Status: {Job_status}")
    else:
        return Job_status
