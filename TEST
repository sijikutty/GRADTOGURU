from playwright.sync_api import Page, expect
from automation_utils import get_testdata, get_secrets,construct_putty_credentials,save_credentials
from smoketest_awsinfra import get_testcase,connect_to_oracleDB
print("hi2")


def test_PS_EGL_DB_connectivity(page : Page):

    print("hi1")
    # Call the function you want to test
    result = connect_to_oracleDB("dev/2807/fgld")

    # Assert the expected result or behavior
    assert result["status"] == True, f"DB connectivity test failed: {result['error']}"

    # Print test success message for visibility
    print(f"Test case test_PS_EGL_DB_connectivity executed successfully.")
    
