# Wait for the iframe to be attached
popup_dialog = page.frame_locator("iframe[name='ptModFrame_0']")
await popup_dialog.wait_for_selector("link[name='View Log/Trace']")

# Click the link within the iframe
await popup_dialog.get_by_role("link", name="View Log/Trace").click()
