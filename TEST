    # Check if the sheet already exists
    sheet_exists = False
    for col_index in range(1, len(column_names) + 1):
        if data_table.GetParameter(col_index).Name == "Sheet1":
            sheet_exists = True
            break
