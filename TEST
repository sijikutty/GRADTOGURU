# Wait for the file to open
time.sleep(5)  # Adjust this delay as needed

# Press the "Enable Content" button
enable_content_button = app.window(title="Microsoft Excel").window(title="Enable Content")
enable_content_button.click()
